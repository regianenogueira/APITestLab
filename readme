/APITestLab
APITESTLAB
│
├── api                          # Código da API ou definições de endpoints.
│   ├── (código da API)         # Implementação da lógica da API, como controladores e serviços.
│
├── teste                        # Pasta dedicada a testes da API.
│   ├── java                     # Código de teste em Java.
│   │   ├── actions              # Métodos que realizam ações específicas na API.
│   │   ├── models               # Classes que representam dados utilizados nos testes.
│   │   ├── runners              # Classes responsáveis por executar os testes.
│   │   ├── services             # Serviços auxiliares com lógica reutilizável.
│   │   └── support              # Classes de suporte com utilitários e helpers.
│   ├── resources                # Arquivos de configuração e outros recursos de teste.
│   │   ├── config.properties     # Configurações como URLs de API e credenciais.
│   │   └── (outros arquivos)    # Outros arquivos necessários para configuração.
│   ├── docker                   # Arquivos para configurar e executar o ambiente Docker.
│   │   ├── Dockerfile           # Instruções para construir a imagem do Docker.
│   │   └── (outros arquivos)    # Scripts ou arquivos de configuração do Docker.
│
└── README.md                    # Documentação do projeto com informações e instruções.


# comandos docker

docker ps  # Para listar os contêineres em execução

docker-compose down # deve parar e remover todos os contêineres definidos no seu docker-compose.yml.

docker-compose up --build # inicia os contêineres definidos no arquivo docker-compose.yml
